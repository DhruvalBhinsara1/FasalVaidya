# =================================================================
# FasalVaidya Environment Configuration
# =================================================================
# Copy this file to .env and fill in your values
# Never commit the actual .env file to git!

# -----------------------------------------------------------------
# Backend API Configuration
# -----------------------------------------------------------------
# The base URL of your Flask backend server
# For local development, use your machine's LAN IP
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.100:5000
EXPO_PUBLIC_API_PORT=5000

# Optional: Override host only (will construct http://HOST:PORT)
# EXPO_PUBLIC_API_HOST=192.168.1.100

# -----------------------------------------------------------------
# Supabase Configuration (for Offline Sync)
# -----------------------------------------------------------------
# Get these from: https://supabase.com → Your Project → Settings → API

# Project URL (format: https://xxxxx.supabase.co)
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Anon/Public Key (safe to expose in client apps)
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-key-here

# -----------------------------------------------------------------
# Feature Flags
# -----------------------------------------------------------------
# Enable/disable sync feature
EXPO_PUBLIC_SYNC_ENABLED=true

# Auto-sync interval in minutes
EXPO_PUBLIC_SYNC_INTERVAL=5

# Enable debug logging
EXPO_PUBLIC_DEBUG=false

# -----------------------------------------------------------------
# Optional: Analytics & Monitoring
# -----------------------------------------------------------------
# EXPO_PUBLIC_SENTRY_DSN=
# EXPO_PUBLIC_ANALYTICS_ID=

# -----------------------------------------------------------------
# Development Notes
# -----------------------------------------------------------------
# 1. For Android emulator: Use 10.0.2.2 instead of localhost
# 2. For iOS simulator: Use localhost or 127.0.0.1
# 3. For physical devices: Use your machine's LAN IP (ipconfig/ifconfig)
# 4. For Expo tunnel mode: Set explicit API host/URL
#
# Find your LAN IP:
#   Windows: ipconfig (look for IPv4 Address)
#   Mac/Linux: ifconfig (look for inet)
#
# Test backend is reachable:
#   curl http://YOUR_IP:5000/api/health
